apiVersion: v1
kind: Pod
metadata:
  name: {{ name }}
  labels:
    app: {{ app_label }}
spec:
  containers:
  - name: {{ container_name }}
    image: {{ image }}
    ports:
    - containerPort: {{ container_port }}
